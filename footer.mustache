    <nav>
        <span>
            {{#prev}}<a href="{{prev.url}}">&larr; {{prev.text}}</a>{{/prev}}
        </span>
        <span>
            {{#next}}<a href="{{next.url}}">{{next.text}} &rarr;</a>{{/next}}
        </span>
    </nav>
    <footer>
        {{^index}}<a href="{{blog.url}}">{{blog.title}}</a>{{/index}}
        <a href="./archive.html">Archive</a>
    </footer>
</body>
</html>
