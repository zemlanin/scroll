<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <form method="POST" action="">
        <textarea name="text">
            {{#post}}
                {{{post.text}}}
            {{/post}}
        </textarea>
        <button type="submit">
            preview
        </button>
    </form>
    <hr />
    {{#post}}
        <article>
            {{{post.html}}}
        </article>
    {{/post}}
</body>
</html>
