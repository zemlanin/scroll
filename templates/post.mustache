{{> header}}

{{#post}}
<article>
  {{#htmlTitle}}{{{htmlTitle}}}{{/htmlTitle}}
  {{{html}}}

  {{^internal}}
  <time datetime="{{created}}">
    <a href="{{{url}}}" title="{{created}}">
      {{createdDate}}
    </a>
  </time>
  {{/internal}}
</article>
{{/post}}

{{> footer}}
